meters:
  export:
    enabled: true
    kafka:
      enabled: true
      bootstrap.servers: "kafka-1:9092"
      topic: qalipsis-meters
      linger.ms: 3000
      batch-size: 500
      configuration:
        delivery.timeout.ms: 14000

